<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RecipeApp" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:buttonStyle">@style/Button</item>
        <item name="android:actionBarStyle">@style/Toolbar</item>

    </style>
    <style name="Base.Theme.RecipeApp1" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/braun1</item>
        <item name="android:buttonStyle">@style/Button</item>
        <item name="android:actionBarStyle">@style/Toolbar</item>
    </style>
    <style name="Toolbar">
        <item name="android:background">@color/braun1</item>
    </style>
    <style name="Button">
        <item name="android:backgroundTint">@color/braun1</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">20dp</item>
    </style>
</resources>